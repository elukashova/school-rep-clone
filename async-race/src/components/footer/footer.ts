import './footer.styles.css';
import BaseComponent from '../base-component';

export default class Footer extends BaseComponent<'footer'> {
  constructor() {
    super('footer', undefined, 'footer');
  }
}
